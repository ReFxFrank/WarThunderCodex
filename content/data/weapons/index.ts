import type { Weapon } from "@/lib/schema";

/**
 * Gun families shared across the roster (§3). Seeded in Phase 5 alongside the
 * vehicles that mount them. Add one file per family or per nation and spread here.
 */
export const weapons: Weapon[] = [];
