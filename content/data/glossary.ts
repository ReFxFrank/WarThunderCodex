import type { GlossaryTerm } from "@/lib/schema";

/**
 * The acronym + term reference the rest of the site links into. Populated with
 * original, concise definitions in Phase 2 (the knowledge backbone).
 */
export const glossary: GlossaryTerm[] = [];
